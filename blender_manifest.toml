schema_version = "1.0.0"

id            = "mcp_socket"
version       = "2.5.0"
name          = "MCP Socket"
tagline       = "Local MCP bridge for Blender, wire-compatible with blender-mcp"
maintainer    = "Maksim Kovalev"
type          = "add-on"

blender_version_min = "4.2.0"

license       = ["SPDX:GPL-3.0-or-later"]

tags          = ["Pipeline"]

website = "https://github.com/abyrvalg379/mcp-socket"

[permissions]
# TCP server on localhost:9876 for the MCP client.
network = "Hosts a local TCP server on 127.0.0.1 so MCP clients can query the scene."
