schema_version = "1.0.0"

id            = "zcode_mcp"
version       = "1.2.2"
name          = "ZCode MCP"
tagline       = "Blender bridge for ZCode — wire-compatible with blender-mcp 1.6.x"
maintainer    = "Maksim Kovalev"
type          = "add-on"

blender_version_min = "4.2.0"

license       = ["SPDX:GPL-3.0-or-later"]

tags          = ["Pipeline"]

website = "https://github.com/abyrvalg379/zcode-mcp"

[permissions]
# TCP server on localhost:9876 for the ZCode/blender-mcp client.
network = "Hosts a local TCP server on 127.0.0.1 so ZCode (or blender-mcp) can query the scene."
